{
  "name": "nasa_project",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "server": "npm run watch --prefix server",
    "client": "npm start --prefix client",
    "watch": "cls && npm run client && npm run server ",
    "deploy": "npm run build --prefix client && npm run cluster --prefix server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.4.5",
    "mongoose": "^8.6.3",
    "morgan": "^1.10.0"
  }
}
